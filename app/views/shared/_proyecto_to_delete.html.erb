<%= form_with(model: proyecto, url: proyecto_delete_path(proyecto), method: :delete, data: { confirm: '¿Estás seguro de que deseas eliminar este proyecto?' }) do |form| %>
  <%= form.submit 'Eliminar', class: 'btn btn-danger' %>
<% end %>