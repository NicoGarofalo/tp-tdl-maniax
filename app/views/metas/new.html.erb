<h1>Crear Meta</h1>

<%= form_for @meta, url: metas_path, method: :post do |f| %>

  <div>
    <%= f.label :proyecto_id %>
    <%= f.collection_select :proyecto_id, Proyecto.all, :id, :nombre %>
  </div>

  <div>
    <%= f.label :fecha_vencimiento %>
    <%= f.date_field :fecha_vencimiento %>
  </div>

  <div>
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div>
    <%= f.label :descripcion %>
    <%= f.text_area :descripcion %>
  </div>

  <div>
    <%= f.submit "Crear Meta" %>
  </div>
<% end %>